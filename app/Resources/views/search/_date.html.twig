{{ form_start(form, {'action': path('date'), 'method': 'POST', attr: {id: 'search', novalidate: 'novalidate'}}) }}

<div class="modal-body">
    <div class="row">
        {% if app.session.get('_date') is not null %}
        <div class="col-md-12 col-xs-12 text-center">
            <small>Trenutno podešen na {{ app.session.get('_date')|date('d-m-Y') }}</small>
        </div>
        {% endif %}
        <div class="col-md-12 col-xs-12">
            {{ include('default/_flash.html.twig') }}

            {{ form_errors(form) }}

            <div class="col-md-12 col-sm-12 col-xs-12">
                {{ form_row(form.date) }}
            </div>

            {{ form_rest(form) }}
        </div>
    </div>

</div>
<div class="modal-footer">
    <button type="submit" id="time_reset" name="time[reset]" class="btn btn-secondary">Isključi</button>
    <button type="submit" id="time_submit" name="time[submit]" class="btn btn-primary">Uključi</button>
</div>

{{ form_end(form) }}

<script>
    $('.datepicker').datepicker({
        format: 'dd-mm-yyyy',
        autoclose: true,
        endDate: "0d",
        todayHighlight: true
    });
</script>
